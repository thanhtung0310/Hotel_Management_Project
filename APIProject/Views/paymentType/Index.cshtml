@model IEnumerable<DatabaseProvider.paymentType>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.payment_type_name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.payment_type_name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.payment_type_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.payment_type_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.payment_type_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
