@model IEnumerable<DatabaseProvider.reservation>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.customer_id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.expected_check_in_date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.day_stay_number)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.expected_room_type_id)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.customer_id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.expected_check_in_date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.day_stay_number)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.expected_room_type_id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.reservation_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.reservation_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.reservation_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
