@using Entity
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@{
    ViewData["Title"] = "Get customer Information by Contact Number";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@model cus_info

<h1>Get customer's information by Contact Number <a asp-action="Index" class="btn btn-sm btn-primary">Back</a></h1>
<h3>@ViewBag.StatusCode</h3>
<form method="post">
    <div class="form-group">
        <label for="search_string">Contact number:</label>
        <input class="form-control" name="search_string" />
    </div>
    <div class="text-center panel-body">
        <button type="submit" class="btn btn-sm btn-primary">Get Customer</button>
    </div>
</form>

@if (Model != null)
{
    <h2>Customer's information</h2>
    <table class="table table-sm table-striped table-bordered m-2">
        <thead>
            <tr>
                <th>Customer's ID</th>
                <th>Customer's first name</th>
                <th>Customer's last name</th>
                <th>Customer's home address</th>
                <th>Customer's contact number</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>@Model.customer_id</td>
                <td>@Model.customer_first_name</td>
                <td>@Model.customer_last_name</td>
                <td>@Model.customer_address</td>
                <td>@Model.customer_contact_number</td>
            </tr>
        </tbody>
    </table>
}