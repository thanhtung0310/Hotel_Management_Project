@model IEnumerable<DatabaseProvider.roomStatus>
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.room_status_id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.room_status_name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.room_status_id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.room_status_name)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.room_status_id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.room_status_id">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.room_status_id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
